/**
 * ============================================================
 * 原石航路 Studio
 * /lp — 案内
 * ============================================================
 */

import LandingClient from "@/components/lp/landing-client";

export const metadata = {
    title: "原石航路 Studio — 書く、調べる、誰かと。",
    description:
        "資料も執筆室もひとつに。誰かと同じ場所で書ける、小説のための制作道具。",
};

export default function LandingPage() {
    return <LandingClient />;
}
