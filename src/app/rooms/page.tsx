/**
 * ============================================================
 * 原石航路 Studio
 * /rooms — コミュニティー
 *
 * 住所は /rooms のまま。
 * 既に配った URL や、頁の中の行き先が生きている。
 * ============================================================
 */

import CommunityClient from "@/components/community/community-client";

export default function CommunityPage() {
    return <CommunityClient />;
}
