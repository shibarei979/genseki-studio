/**
 * ============================================================
 * 原石航路 Studio
 * /login — ログイン
 * ============================================================
 */

import LoginClient from "@/components/auth/login-client";

export default function LoginPage() {
    return <LoginClient />;
}
