import AdminPlansClient from "@/components/admin/admin-plans-client";

/**
 * 運営 — 会員（サブスクリプション）
 *
 * ★ まだ読む人には出していない。
 *   中身は、運営の鍵でしか読めない口から取っている。
 */
export const dynamic = "force-dynamic";

export default function Page() {
    return <AdminPlansClient />;
}
